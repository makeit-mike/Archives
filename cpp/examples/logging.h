#pragma once // Means only compile the one time

// basics to H files.
// Header files are useful for linking functions between other cpp files.

// The actual point of headers, is to use a compiled library in another program without disclosing the actual source code.

void Log(const char *input);
