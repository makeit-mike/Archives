(ns algorithms.core
  (:gen-class))


(comment
  this main point does. . . nothing.
  take a look around the other files.
)

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))


